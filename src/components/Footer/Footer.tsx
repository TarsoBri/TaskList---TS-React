import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>
        To Do list - React + TS <span>@2024</span>
      </p>
    </footer>
  );
};

export default Footer;
